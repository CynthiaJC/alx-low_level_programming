#include <main.h>
/**
 * prints_alphabet_x10 - Prints the alphabet 10 times 
 * 
 * Return: Always 0 (success) 
 */
void print_alphabet_x10(void)
{
	char ch;
	int i;

	i = 0;

	for (i < 10)
	{
		ch = 'a'
		for (ch <= 'z')
		{
			_putchar('\n');
			ch++;
		}
		_putchar('\n');
		i++;
	}
}
